# Supabase Configuration
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
# IMPORTANT: Service role key should ONLY be set in Supabase Edge Function secrets
# DO NOT put SUPABASE_SERVICE_ROLE_KEY in .env files!

# Stripe Configuration (Public keys only for frontend)
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
# IMPORTANT: Stripe secret key should ONLY be in Edge Function secrets
# DO NOT put STRIPE_SECRET_KEY in .env files!

# Application URLs
VITE_APP_URL=http://localhost:8080

# Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_AI_RECOMMENDATIONS=false
VITE_ENABLE_DEV_ROUTES=false # Set to true only for development/testing

# Security Configuration
# IMPORTANT: Generate secure keys for production using: npx tsx src/lib/security/generateKeys.ts
# Never commit actual keys to version control!
TWO_FA_ENCRYPTION_KEY=generate-secure-key-for-production
JWT_SECRET=generate-secure-key-for-production
WEBHOOK_SECRET=generate-secure-key-for-production